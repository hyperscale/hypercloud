import { ContainerPortPipe } from './container-port.pipe';

describe('ContainerPortPipe', () => {
  it('create an instance', () => {
    const pipe = new ContainerPortPipe();
    expect(pipe).toBeTruthy();
  });
});
